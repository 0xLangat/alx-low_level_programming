#include <stdio.h>

/**
 * main - prints the name of the program
 *
 * Return: On success 0
 * On failure other number
 */
int main(int argc, char* argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);

	return (0);
}
