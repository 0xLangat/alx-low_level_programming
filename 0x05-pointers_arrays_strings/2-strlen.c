#include "main.h"
#include <string.h>

/**
 * _strlen - find the length of a string
 *
 * @s: the string to be checked
 *
 * Return: Length of the string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
